The existing split makes sense:
• ddl → structure definition (CREATE, ALTER, DROP, etc.)
• dml → data modification (INSERT, UPDATE, DELETE)
• dql → data retrieval / read operations (SELECT, and things like WITH queries, etc.)